#!/bin/bash
# Deprecated: legacy prebuilt demo removed with docker/legacy (2023).
# Use: docker compose up -d   (from repository root)
# Then follow README "Liveblog local setup (recommended)".

set -ue
echo "This script is deprecated. From the repository root run: docker compose up -d"
echo "See README.md and docker-compose.yml"
exit 1
