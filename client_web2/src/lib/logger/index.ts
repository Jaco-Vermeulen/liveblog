/**
 * @deprecated Import from `@/mechanisms/request-logger` instead.
 * Re-export kept for Phase 0 import paths during migration.
 */
export { logger, __resetLoggerForTests } from '@/mechanisms/request-logger';
export type { ApiLogEntry, IoChannel, LogDirection, LogLevel, Logger } from '@/mechanisms/request-logger';
